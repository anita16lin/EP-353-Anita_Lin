#include <stdio.h>

int main()
{
    FILE *file;
    file = fopen("LoremIpsum.txt", "r");
    if(file == NULL){
        printf("File cannot be opened or does not exist");
        return 1;
    }
    
    char string[50];

    while
    return 0;
}